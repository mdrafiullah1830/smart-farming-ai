# `docs/`

Documentation only. Static HTML/JS/data assets have been removed; they
live in `frontend/web/`.

Files kept here:
- `API_DOCUMENTATION.md` — endpoint reference for the legacy
  `frontend/server.js`. Will move into the Worker once its API stabilizes.
- `ARCHITECTURE.md` — historical architecture notes. The current
  source of truth is `../ARCHITECTURE.md`.
- `CLOUD_ARCHITECTURE.md` — historical cloud notes. Superseded by
  `../ARCHITECTURE.md`.
